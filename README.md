# edu-onnx-runtime

Educational ONNX inference runtime in C++ (`eduort`).

Scaffolding and design land via pull requests — start with **PR1** (CMake skeleton).

- Design: will live at `docs/design.md` after PR1
- License: Apache-2.0 (see `LICENSE`)
